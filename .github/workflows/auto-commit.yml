name: Auto Commit

on:
  schedule:
    - cron: "0 10 * * *"   # runs every day at 10:00 UTC
  workflow_dispatch:        # allows manual run

# Ensure this workflow can push commits back to the repository
permissions:
  contents: write

jobs:
  auto-commit:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3
        with:
          # Personal Access Token stored as a repository secret
          token: ${{ secrets.PAT_TOKEN }}

      - name: Make changes
        run: |
          echo "Updated at $(date)" >> auto-update-log.txt

      - name: Commit changes
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"
          git add .
          git commit -m "Automated commit" || echo "No changes to commit"
          git push origin HEAD:main
